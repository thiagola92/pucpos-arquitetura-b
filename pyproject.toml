[project]
name = "pucpos-arquitetura-b"
version = "0.1.0"
description = "Backend para a pós-graduação"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.121.1",
]
